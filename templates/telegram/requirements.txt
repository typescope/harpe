# Model providers
anthropic>=0.40
openai>=2.21

# Telegram reply rendering
markdown-it-py

# Document, image, and OCR capabilities wired by sandbox/SandboxRuntime.jo
pypdfium2>=5
python-docx
openpyxl
pillow

# RapidOCR ships no inference engine, so the engine it runs the models on is a
# requirement of its own. onnxruntime 1.22 is the floor that still has a wheel
# for Python 3.10, this project's oldest supported interpreter.
rapidocr>=3
onnxruntime>=1.22
