# The WSGI server this agent runs on. Harpe installs none: an `Application` is
# a plain WSGI callable, and the deployment chooses what serves it.
waitress>=3

# Model providers
anthropic>=0.40
openai>=2.21

# Telegram reply rendering
markdown-it-py
