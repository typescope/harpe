namespace sandbox.guest

import jo.IO.stdout
import sandbox.api

// This placeholder proves the sandbox links. runCode compiles each generated
// program in a temporary directory without modifying this file.
def runTask(): Unit receives stdout =
  println "Hello from the sandbox."
