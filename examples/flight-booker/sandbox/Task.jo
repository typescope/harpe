//[ Task.jo — the sandbox's default guest task.
  !
  ! The entry point the agent's `runCode` tool replaces each turn with the LLM's
  ! generated program. This default just confirms the sandbox is wired correctly.
//]

namespace sandbox.guest

import sandbox.api.*

def runTask(): Unit receives IO.stdout, duffel =
  println "flight-booker sandbox ready — Duffel API wired"
