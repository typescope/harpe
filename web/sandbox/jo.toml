jo = "0.12"

# The api module depends only on the pure `caps` interfaces: the guest (which
# depends on api) can name the capabilities, never the harpe implementations.
[module.api]
kind = "lib"
platform = "python"
src = ["SandboxAPI.jo"]
depth = 1

packages = [{ name = "harpe-caps", version = "0.7" }]

[module.runtime]
kind = "lib"
platform = "python"
enable-ffi = true
src = ["SandboxRuntime.jo"]
depth = 2

modules = ["api"]
packages = [
  { name = "harpe", version = "0.7" },
]

[module.guest]
kind = "app"
platform = "python"
src = ["Task.jo"]
depth = 2

modules = ["api", { id = "runtime", link = true }]
packages = [{ name = "harpe-caps", version = "0.7" }]

links = [
  { from = "jo.main", to = "sandbox.runtime.main" },
  { from = "sandbox.api.runTask", to = "sandbox.guest.runTask" },
]
