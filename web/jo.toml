jo   = "0.11"
name = "harpe-web"

[package]
version = "0.1.0"
runtime = "python"

[main.dependencies]
harpe = { path = "../agent" }

[commands]
start = "jo build --spec sandbox/guest/jo.toml && jo run"
