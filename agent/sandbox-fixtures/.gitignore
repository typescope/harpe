# Test fixtures resolve against whatever compatible compiler version is active,
# so their lock files are transient build artifacts (regenerated by `jo build`),
# not pinned in the repo. This keeps the fixtures working across compatible
# minor/patch versions. (The real packages — agent, cli, sandbox, example —
# keep their committed locks.)
jo.lock
