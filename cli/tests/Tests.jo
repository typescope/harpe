//[ Tests.jo
  !
  ! The `jo test` entry point for the `harpe-cli` package.
//]

import jo.IO.stdout
import SessionLogTest

def main(): Unit receives stdout =
  println "session log (unit):"
  SessionLogTest.run()

  println "all harpe-cli tests passed"
