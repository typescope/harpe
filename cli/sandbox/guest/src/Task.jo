namespace UserTask

def isPrime(n: Int): Bool =
  if n < 2 then false
  else
    for i in 2 to (n - 1) do
      if n % i == 0 then return false
    true

def runTask(): Unit =
  val primes = (1 to 99).toList().select(x => isPrime(x))
  println(primes.join(", "))