//[ Rect.jo
  !
  ! A rectangular region in surface pixels (top-left origin) — a target to flow
  ! text into. Disjoint rects (say two columns) are filled in order.
//]

namespace harpe.caps.drawing

//[ A rectangular region. //]
class Rect(x: Float, y: Float, width: Float, height: Float)
