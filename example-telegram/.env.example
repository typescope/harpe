# Copy to .env and fill in. The loop loads .env on startup.

# --- Telegram (required) ---
# A bot token from @BotFather (talk to it in Telegram, /newbot).
TELEGRAM_BOT_TOKEN=

# Comma-separated chat ids allowed to use the agent. A bot is publicly
# reachable and this agent RUNS CODE, so access is closed by default: leave this
# empty and every message is rejected. Message the bot once — it replies with
# your chat id — then add it here (e.g. 12345678 or -1009998887 for a group).
TELEGRAM_ALLOWED_CHAT_IDS=

# This example uses the keyless `echo` model by default (see the
# harpe.telegram.Config.model link in jo.toml), so no LLM key is needed to try
# the plumbing. To use a real model, drop that link and set one of the sections.

# --- Anthropic (default when the echo link is removed) ---
# PROVIDER=anthropic
# MODEL=claude-opus-4-6
# ANTHROPIC_API_KEY=

# --- OpenAI / OpenAI-compatible ---
# PROVIDER=openai
# MODEL=gpt-4o
# OPENAI_API_KEY=
# OPENAI_BASE_URL=   # optional: Groq, Together AI, local llama.cpp, etc.
