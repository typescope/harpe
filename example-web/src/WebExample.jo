//[ WebExample.jo
  !
  ! Wires the web agent to the keyless `echo` model so the skeleton runs with no
  ! API key — overriding `harpe.web.model` via this agent's jo.toml. Remove the
  ! link to use the Anthropic default instead.
//]

namespace WebExample

import harpe.models.*

def model(): Model receives IO.stdout = echo()
