jo   = "0.11"
name = "hello-web"

[main]
target = "python"

[main.dependencies]
harpe-web = { path = "../web" }

[main.links]
"jo.main" = "harpe.web.main"
# Use the keyless echo model so the skeleton runs with no API key. Drop this
# link to fall back to the Anthropic default (set ANTHROPIC_API_KEY in .env).
# "harpe.web.Config.model" = "WebExample.model"
