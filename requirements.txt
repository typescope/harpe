# Python backends exercised by the Harpe media tests
pypdfium2>=5
python-docx
openpyxl
pillow

# Real provider SDKs exercised against the tests' local SSE server
anthropic>=0.40
openai>=2.21

# The CLI agent renders its replies with this, so the e2e suite that runs the
# shipped CLI needs it too (it is also in cli/requirements.txt, for the agent)
markdown-it-py

# The OCR backend. Not optional any more: the compiler collects every import at
# top level and binds them eagerly, so the module is imported whether or not a
# test reaches the code that uses it.
rapidocr>=3
onnxruntime>=1.22
