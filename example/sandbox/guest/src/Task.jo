namespace UserTask

import jo.IO.stdout
import HarpeCaps.*

// The LLM rewrites this each turn. Placeholder confirms the build.
def runTask(): Unit receives stdout, skills, logger =
  println("ready")
