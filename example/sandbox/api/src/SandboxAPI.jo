namespace SandboxAPI

import jo.IO.stdout
import HarpeCaps.*

// The agent's contract: the entry point the LLM implements each turn, and the
// capabilities it receives. Add interfaces / widen `receives` to grant more.
defer def runTask(): Unit receives stdout, skills, logger
