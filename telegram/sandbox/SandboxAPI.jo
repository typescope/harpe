namespace sandbox.api

import jo.IO.stdout

// The agent's contract: the entry point the LLM implements each turn, and the
// capabilities it receives. Grant more by declaring capability interfaces and
// widening `receives` — their implementations get the host-side `Sandbox`, the
// LLM only gets the capability.
defer def runTask(): Unit receives stdout
